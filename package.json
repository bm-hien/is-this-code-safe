{
  "name": "itcs-web-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the ITCS local browser analyzer.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build_web_editor.mjs",
    "test:web": "node --test web/tests/*.test.mjs",
    "check:web": "npm run build:web && npm run test:web"
  },
  "devDependencies": {
    "dompurify": "3.4.13",
    "esbuild": "0.28.2",
    "monaco-editor": "0.56.0"
  },
  "overrides": {
    "dompurify": "3.4.13"
  }
}
